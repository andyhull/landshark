    <div class="container"> <!-- main container -->
        <div class="well">
        	<h2>SF Team reminders</h2>
            <p>Text messaging system for the Code for America SF Team</p>
            <%= link_to 'Add new user', new_recipient_path, :class => 'btn btn-primary' %>
            <%= link_to 'View programs', programs_path, :class => 'btn' %>
            <%= link_to 'View reports', reports_path, :class => 'btn' %>
    	</div>
        <div class="row-fluid">
            <div class="span3">
            <%= render(:partial => 'ataglance') %>
            </div>
            <div class="span9">
             <%= render(:partial => 'messages', :collection => @recents, :as => :recents) || 'There are no recent messages' %>

            </div>
        </div>

</div>
